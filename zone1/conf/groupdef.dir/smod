; conf/groups/smod

seesysoplogarena
; bypass checksums
bypasssecurity

; what groups can this group control
higher_than_mod

; prefix chars
prefix_>

privcmd_info

cmd_aa
cmd_za

cmd_attmod

cmd_moveflag

cmd_watchdamage
privcmd_watchdamage

; the following three should always go together
cmd_quickfix
cmd_getsettings
changesettings

cmd_putmap

cmd_userdbadm

privcmd_send

